export enum ChatRole {
  SYSTEM = "system",
  USER = "user",
  ASSISTANT = "assistant",
}

export enum ChatType {
  FOLLOW_UP = "followUp",
  DISCUSSION = "discussion",
}

export enum ChatModelType {
  QUESTION = "question",
  SECTION = "section",
}
